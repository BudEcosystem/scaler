{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "scaler.fullname" . }}-manager-rolebinding
  labels:
    {{- include "scaler.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "scaler.fullname" . }}-manager-role
subjects:
  - kind: ServiceAccount
    name: {{ include "scaler.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
